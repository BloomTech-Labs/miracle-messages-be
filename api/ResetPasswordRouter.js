const express = require("express"); 
const router = express.Router(); 

// To find the req.body of id and email to make sure that the email existis in the database 
router.get("/", (req, res) => {

})


// To replace volunteers old password with new one 
router.put("/", (req, res) => {

})